# HTTP
PORT=4000
# CORS_ORIGIN=http://localhost:5173

# PostgreSQL (or use DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=app
DB_PASSWORD=app
DB_NAME=app

# DATABASE_URL=postgres://user:pass@host:port/db

